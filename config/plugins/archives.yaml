enabled: true
built_in_css: true
date_display_format: 'F Y'
show_count: true
limit: 12
taxonomy_names:
  month: archives_month
  year: archives_year
order:
  by: date
  dir: desc
filters: {  }
filter_combinator: or
page_specific_config:
  -
    route: /blog
    filters:
      page@: /blog
    filter_combinator: or
    order_by: null
    order_dir: null